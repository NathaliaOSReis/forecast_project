# Configurações da API Open-Meteo
OPENMETEO_URL=https://api.open-meteo.com/v1/forecast

# Configurações do servidor FastAPI
# Para execução local:
API_HOST=127.0.0.1
API_PORT=8000

# Para execução com Docker:
# API_HOST=api
# API_PORT=8000

# Credenciais e chaves de API (substitua pelos valores reais no arquivo .env)
API_KEY=
API_SECRET=
DATABASE_URL=
JWT_SECRET=

# Variáveis de ambiente para serviços externos
WEATHER_API_KEY=
MAPS_API_KEY=

# IMPORTANTE:
# 1. Este é um arquivo de exemplo. Copie para .env e preencha com valores reais
# 2. Nunca inclua valores reais ou senhas neste arquivo
# 3. O arquivo .env deve estar no .gitignore para não ser versionado
# 4. Em produção, considere usar variáveis de ambiente ou serviços de gestão de segredos


